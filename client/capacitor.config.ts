import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bridev.miscuentas',
  appName: 'Mis Cuentas',
  webDir: 'dist',
  android: {
    backgroundColor: '#f1f5f9',
  },
};

export default config;