{
  "name": "cuentas",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev -w client",
    "build": "npm run build -w client",
    "test": "npm test -w client"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}